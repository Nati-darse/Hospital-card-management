package com.hospital.card.config;

/**
 * JWT filter disabled for Phase 1. Kept as a placeholder so the
 * file can be re-enabled later if needed.
 */
public final class JwtAuthenticationFilter {
    // disabled placeholder
}